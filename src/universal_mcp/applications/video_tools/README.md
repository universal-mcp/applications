# VideoToolsApp MCP Server

An MCP Server for the VideoToolsApp API.

## 🛠️ Tool List

This is automatically generated from OpenAPI schema for the VideoToolsApp API.


| Tool | Description |
|------|-------------|
| `stitch_videos` | Stitches multiple video files together into a single video file in the order provided. |
| `trim_video` | Trims a video to a specific time range, creating a shorter clip from the original video. |
| `get_video_info` | Retrieves detailed information about a video file including duration, resolution, fps, codec, and file size. |
| `resize_video` | Resizes a video to specified dimensions or scale factor with optional aspect ratio preservation. |
| `extract_audio` | Extracts the audio track from a video file and saves it as a separate audio file. |
| `add_audio` | Adds or replaces the audio track in a video file with audio from a separate audio file. |
| `convert_video` | Converts a video file to a different format with specified codecs. |
| `change_video_speed` | Changes the playback speed of a video by a specified factor, making it faster or slower. |
| `create_slideshow_from_images` | Creates a video slideshow from a sequence of images with configurable display duration and optional transitions. |
